#!/bin/bash
set -euo pipefail

echo "--- :cloudformation: Running cfn-lint with .cfnlintrc.yml configuration"

# Run cfn-lint without specifying files, letting it use .cfnlintrc.yml
docker run --rm -v "$(pwd):/data" -w "/data" giammbo/cfn-lint
